# `.gitattributes` file which reclassifies `.html` files as php, for github to ...
# ...show the repo language is PHP(from unit test files) instead of HTML(from code coverage files):
*.html linguist-language=PHP